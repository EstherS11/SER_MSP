program: minimal_sweep_train.py
method: random
metric:
  name: best_valid_f1
  goal: maximize

parameters:
  batch_size:
    values: [4, 8, 16]
  
  learning_rate:
    values: [0.001, 0.0001]
  
  focal_gamma:
    values: [1.5, 2.0, 2.5]
  
  optimizer:
    values: ['adam', 'adamw']

run_cap: 5
